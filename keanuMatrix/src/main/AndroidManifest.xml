<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.guardianproject.keanu.matrix" >


    <application>

        <service
            android:name="info.guardianproject.keanu.matrix.plugin.MatrixImPlugin"
            android:exported="false" >
            <intent-filter>
                <action android:name="info.guardianproject.keanu.plugin" />
            </intent-filter>

            <meta-data
                android:name="info.guardianproject.keanu.plugin.provider_name"
                android:value="MATRIX" />
            <meta-data
                android:name="info.guardianproject.keanu.plugin.provider_full_name"
                android:value="Keanu Matrix Plugin" />
            <meta-data
                android:name="info.guardianproject.keanu.plugin.signup_url"
                android:value="https://matrix.org" />
        </service>

    </application>
</manifest>
